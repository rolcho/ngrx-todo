{
  "default": {
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "dryRun": false,
    "paths": ["features/"],
    "require": ["features/**/*.steps.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["progress"]
  }
}
